{
	"name": "images",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --name=images",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.10.9",
		"typescript": "^5.5.2",
		"vitest": "~4.0.12",
		"wrangler": "^4.49.1"
	}
}
